import 'package:firebase_core/firebase_core.dart';

/// PLACEHOLDER — replace this file by running `flutterfire configure`
/// (see mobile/README.md → Firebase setup). Until then, [currentPlatform]
/// throws and `main()` skips Firebase init so the rest of the app still runs.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => throw UnsupportedError(
        'firebase_options.dart is a placeholder. Run `flutterfire configure` '
        'to generate the real configuration before using push notifications.',
      );
}
